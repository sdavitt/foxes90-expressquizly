// collecting all of the models created in their own little individual pages

module.exports = {
    User: require('./user.model'),
    Quiz: require('./quiz.model'),
    Question: require('./question.model'),
    Submission: require('./submission.model')
}